import {
  FormControl,
  FormErrorMessage,
  FormHelperText,
  FormLabel,
  Box,
  Heading,
} from '@chakra-ui/react';

const Login = () => {
  return (
    <Box as="form">
      <Heading>Login</Heading>
    </Box>
  );
};
export default Login;
